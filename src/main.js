// Central entry imported by ALL pages (index.html and every /pages/*.html)

// Shared auth (if you still keep it)
import '../utils/auth.js';

// Theme + paw prints
import './theme.js';

// General site scripts (original root script)
import '../script.js';
