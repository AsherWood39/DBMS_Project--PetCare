{
  "name": "petcare-monorepo-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix server install",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
